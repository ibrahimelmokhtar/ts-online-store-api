type User = {
	id?: string;
	first_name: string; // user's first name.
	last_name: string; // user's last name.
	user_name: string; // user's user name.
	email: string; // user's email.
	password: string; // user's password.
};

export default User;
